package com.zhangcy.java.data.structure.ch09;

/**
 * 用于记录当前节点是父节点的左节点还是右节点
 * @author zhangcy
 */
public enum LrEnum {

    /**
     * 当前节点是父亲节点的右子节点
     */
    RIGHT,

    /**
     * 当前节点是父亲节点的左子节点
     */
    LEFT;
}
